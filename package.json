{
  "name": "github-user-search-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "format": "npx prettier --write .",
    "build": "mkdirp ./dist && browserify ./scripts/app.js > ./dist/bundle.js",
    "style": "npx tailwindcss -i ./tailwind.css -o ./dist/tailwind.css",
    "watch": "watchify ./scripts/app.js -o ./dist/bundle.js -d -v",
    "start": "npm run clean && npm run build && npm run style && live-server & npm run watch"
  },
  "author": "Tuan Ngo",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.4"
  },
  "devDependencies": {
    "mkdirp": "^1.0.4",
    "prettier": "2.4.0",
    "rimraf": "^3.0.2",
    "watchify": "^4.0.0"
  }
}
